/* Generated by Together */

#ifndef CQUEUE_H
#define CQUEUE_H

#include <afxmt.h>

#include "RtnCode.h"

class CMsgItem;

class CQueue {
	CCriticalSection CS;
public:

	CQueue();

	~CQueue();

	void Clear();

	//	short AddHead(CMsgItem * pMsg);

	CMsgItem* RemoveHead();

	CRtnCode::RCODE AddHead(CMsgItem* pMsg);
	CRtnCode::RCODE AddTail(CMsgItem* pMsg);

	//	CMsgItem * RemoveTail();

private:

	/** Container of CMsgItem */
	CPtrList pList;
public:
	CRtnCode::RCODE  AddH(CMsgItem* m);
	CRtnCode::RCODE  AddT(CMsgItem* m);
	long index;
	int GetCount(void);
	CMsgItem* Remove(short ID);
};
#endif //CQUEUE_H
