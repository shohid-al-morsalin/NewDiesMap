/* Generated by Together */

#ifndef CLINKS_H
#define CLINKS_H

#include <afxmt.h>
#include "Queue.h"
#include "MsgItem.h"
#include "RtnCode.h"

/** Depository of all message(CMsgItem) for a single channel. */
class CLinks {
	CCriticalSection CS;
public:
	CMsgItem* Check();

	CLinks();

	void Clear();

	~CLinks();

	/** Get an asynchronous message from InQue */
	CMsgItem* Get();

	/** Put a msg on the InQue after receiving it from the channel */
	CRtnCode::RCODE Put(CMsgItem* pMsg);

	// will call Put(CMsgItem *)
	CRtnCode::RCODE Put(char* pMsg);

	/** Stuff that come in from the channel */
	CQueue InQue;

	/** Stuff that needed to send over the channel */
	CQueue OutQue;

	/** Stuff that come in in response to sending stuff from OutQue */
	CQueue RpyQue;
};
#endif //CLINKS_H
