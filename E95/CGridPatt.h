/* Generated by Together */

#ifndef CGRIDPATT_H
#define CGRIDPATT_H

#include "Pattern.h"
#include "XYPair.h"

class CGridPatt /* : public CPattern*/
{
public:
	CGridPatt();
	virtual ~CGridPatt();

public:
	CPattern* pPatt;
	// 	CPattern dmy;

	short GetGridN() { return ngrid; }
	float GetGridY() { return ngridX; }
	float GetGridX() { return ngridY; }

	void SetGridN(short n) { ngrid = n; }
	void SetGridX(float fGrid) { ngridX = fGrid; }	// For number of points in X direction
	void SetGridY(float fGrid) { ngridY = fGrid; }	// For Number of points in Y direction

	CGridPatt& operator=(CGridPatt& GridPatt);

public:	// virtual functions
	void ClearList();
	void Serialize(CArchive& ar);
	BOOL SetUp(CWnd* pWnd);
	BOOL Generate(float fDiameter);
	BOOL Generate3(float fDiameter, float waferRadius, BOOL edgeExclude); // Morsalin
	BOOL Generate2(int NX, int NY, float X0, float Y0, float sizeX, float sizeY, float fDiameter, BOOL bIgnore);
	virtual void DrawPattern(CDC* pDC, const CRect& rect, float);

	/**
	 * depends on type type 1 = ngrid is grid line type 2 = ngrid is number of lines
	 * type 3 = ngrid is number of concentric rings type 4 = undefine
	 */
	short ngrid;
	/***  Support for different number of lines in X and Y direction*/
	float ngridX;	// For Number of points in X direction
	float ngridY;	// For number of points in Y direction
	BOOL bClearData;
	BOOL bUseMask;
	CXYPair dX, dY;
};

#endif //CGRIDPATT_H
